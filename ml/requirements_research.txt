# ML research experiment dependencies
# Install with: pip install -r ml/requirements_research.txt

# Core data
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0

# ML
scikit-learn>=1.3.0
joblib>=1.3.0

# Deep learning — CPU build (no CUDA required)
# For Windows: torch is installed from pytorch.org CPU index (see TRANSFORMER_RUN_GUIDE.md)
# torch>=2.0.0  # installed separately, see guide

# HTTP (for downloading candles)
requests>=2.28.0

# Testing
pytest>=7.0.0
