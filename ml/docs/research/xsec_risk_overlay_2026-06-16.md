# Risk overlay: vol-targeting (H4) + regime gate (H5) на deployment-гейте — 2026-06-16

Капстоун: применяем валидированные риск-компоненты ПОВЕРХ кросс-секционного ранжировщика
в честной deployment-симуляции (rolling-retrain Ridge, beta_residual таргет, комиссия,
forward 2025-2026). Скрипт: `ml/scripts/xsec_risk_overlay_sim.py`.

| Вариант | FWD cum | FWD maxDD | FWD vol | win | сделок |
|---------|---------|-----------|---------|-----|--------|
| raw (equal-weight) | −0.024 | −0.131 | 0.047 | 0.41 | 22 |
| +vol (инверс-EWMA-vol сайзинг) | 0.000 | −0.093 | 0.050 | 0.50 | 22 |
| +regime (гейт новизны) | +0.031 | −0.096 | 0.041 | 0.44 | 16 |
| **+both** | +0.023 | **−0.085** | 0.045 | 0.56 | 16 |

## Вывод
На нулевом ценовом сигнале доходность ожидаемо ~0 — судим по РИСКУ:
- **vol-сайзинг (H4)** режет просадку −0.131 → −0.093 (даунсайз волатильных имён).
- **режимный гейт (H5)** воздерживается в 6 из 22 периодов (новые режимы) → просадка и vol ниже.
- **оба вместе** — лучшая просадка −0.085 и win 0.56.

Машинерия риск-оверлея доказана и готова. Настоящий тест — с новостным сигналом: тот же путь
сайзит (H4) и гейтит (H5) ранжировщик на `[цена ⊕ новости]`. Малая forward-выборка (22 периода) —
числа шумные, но направление совпадает с механизмом (оверлей может только снижать риск).

## Артефакт
- `ml/scripts/xsec_risk_overlay_sim.py`
