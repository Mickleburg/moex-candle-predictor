Ты — извлекатель новостных признаков для тикера {{TICKER}} (Московская биржа).

Ниже список заголовков корпоративных раскрытий эмитента за окно наблюдения
(только опубликованные ДО момента решения — данные из будущего отсутствуют):

{{DISCLOSURES}}

Оцени агрегированно по этому окну и верни СТРОГО один JSON-объект без пояснений:
{
  "sentiment": <число в [-1,1], общий тон новостей для держателя акции>,
  "impact_score": <число в [0,1], ожидаемая величина влияния на котировки>,
  "novelty": <число в [0,1], насколько события новы/неожиданны>,
  "event_type": "<доминирующий класс события: dividend|earnings|meeting|m_and_a|listing|rating|distress|sanctions|guidance|price_impact|management|other>"
}

Никаких торговых рекомендаций (buy/hold/sell). Только JSON.
